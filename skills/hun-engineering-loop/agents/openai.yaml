interface:
  display_name: "Hun Engineering Loop"
  short_description: "Run Hun-specific engineering workflow"
  default_prompt: "Use $hun-engineering-loop to turn Hun's request into the smallest complete result with direct evidence."
policy:
  allow_implicit_invocation: true
