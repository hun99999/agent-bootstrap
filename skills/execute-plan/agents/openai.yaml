interface:
  display_name: "Execute Plan"
  short_description: "Carry out an approved implementation plan"
  default_prompt: "Use $execute-plan to carry out this approved implementation plan."
policy:
  allow_implicit_invocation: false
